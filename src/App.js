import React from "react";
import Cards from "./componants/profile/Cards";

export default function App() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      <Cards style={{}}></Cards>
    </div>
  );
}
